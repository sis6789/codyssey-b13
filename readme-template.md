# Part 2. 최종 보고서 샘플 (제안 1 기준)

> 아래는 그대로 제출용 문서로 써도 되는 샘플입니다. `>`로 시작하는 인용구 줄은 모두 "이 항목에 무엇을 채워야 하는지" 알려주는 보조 가이드이며, 실제 예시 내용은 인용구가 아닌 일반 본문으로 이미 채워져 있습니다. 제출 전 인용구 줄만 지우면 됩니다.

## 자동화 도구 비교 구현: 신규 문의 이메일 자동 분류·배정 (Make vs Zapier)

작성자: (이름) / 제출일: 2026-07-16

### 1. 사용한 도구

Make, Zapier

> 가이드: 실제 사용한 도구의 정확한 플랜명(무료/유료)까지 함께 적으면 비교 항목 작성이 쉬워집니다.

### 2. 구현 과정 요약

동일한 워크플로우("신규 문의 메일 수신 → 긴급 키워드 여부로 분기 → Google Sheets 이력 기록 + Slack 알림")를 Make와 Zapier에 각각 구현했다. Make에서는 Router 모듈 하나로 두 분기를 한 시나리오 안에 표현했고, Zapier는 Filter가 단일 조건만 지원해 긴급용/일반용 Zap을 각각 만들어 동일한 Trigger를 공유하도록 구성했다. 두 도구 모두 Gmail 커넥터로 새 메일을 감지했고, 분기 결과에 따라 Google Sheets에 이력을 남긴 뒤 Slack으로 담당 채널에 알림을 보냈다.

> 가이드: 실제 구현하며 겪은 이슈(연동 승인 절차, 트리거 감지 지연, 커넥터 이름 차이 등)를 2~3문장 추가하면 "구현 과정"이 더 구체적으로 보입니다.

### 3. 비교 항목

|비교 항목|Make|Zapier|
|---|---|---|
|UI/UX|노드(시각적 캔버스) 기반, 흐름 한눈에 파악 용이|리스트(단계별) 기반, 직관적이나 전체 흐름 파악은 상대적으로 어려움|
|설정 난이도|Router/모듈 개념 학습에 초기 진입장벽 있음|Trigger-Action 순서만 알면 바로 시작 가능|
|연동 서비스 범위|Gmail, Sheets, Slack 등 주요 서비스 폭넓게 지원|동일 서비스 지원하나 세부 트리거 옵션 구성이 다름|
|조건 분기 방식|Router 하나로 다중 분기 표현 가능|Filter는 단일 조건이라 분기마다 별도 Zap 필요|
|무료 플랜 범위|월 1,000 Ops|월 100 Tasks(회사 기준 최신 정책은 공식 페이지 확인 필요)|
|실행 로그 확인 방식|시나리오 History에서 단계별 입출력 데이터 상세 확인|Task History에서 Zap별 실행 결과 확인|

> 가이드: 표는 최소 5개 항목 요건을 충족합니다. 실제 캡처한 화면에서 확인한 수치(무료 플랜 한도 등)로 업데이트하세요.

### 4. 도구별 장단점

Make는 하나의 시나리오 안에서 복잡한 분기와 다중 액션을 시각적으로 관리할 수 있어 조건이 많은 워크플로우에 유리하지만, 처음 접할 때 모듈/라우터 개념을 익히는 데 시간이 걸린다. Zapier는 진입장벽이 낮고 각 단계가 단순 리스트로 표현되어 빠르게 결과물을 만들 수 있지만, 분기가 늘어날수록 Zap을 여러 개로 쪼개야 해서 관리 포인트가 늘어난다.

### 5. 적합 상황 의견

조건 분기가 2개 이상이거나 하나의 흐름 안에서 여러 갈래를 한눈에 관리해야 하는 업무라면 Make가 더 적합하다고 판단했다. 반대로 분기 없이 단순한 Trigger-Action 자동화를 빠르게 만들어야 하는 상황, 혹은 노코드 도구를 처음 접하는 팀원이 유지보수까지 맡아야 하는 경우라면 Zapier의 직관적인 리스트형 UI가 더 유리하다.

> 가이드: "어떤 상황에서 적합한지"는 정답이 없는 항목입니다. 본인이 실제로 구현하며 느낀 체감 난이도, 팀 상황을 근거로 서술하세요.

---

## 자유 주제 자동화: 부정 리뷰 실시간 감지 및 AI 요약 알림 (제안 3 기반)

### 1. 반복 업무 정의

고객 리뷰가 Google Sheets에 새로 쌓일 때마다 사람이 직접 읽고 부정적인 내용인지 판단해 요약한 뒤 담당자에게 전달하던 작업을, 별점 기준 자동 분기와 AI 요약으로 대체한다.

### 2. 도구 선정 및 이유

Make를 선정했다. 이유는 (1) Router로 별점 조건 분기를 한 시나리오 안에서 처리할 수 있고 (2) OpenAI/Anthropic 모듈이 기본 제공되어 별도 API 연동 설정 없이 AI 요약 Action을 추가할 수 있으며 (3) Error Handler 라우트를 붙여 실패 알림·대체 저장까지 하나의 시나리오로 완결할 수 있고 (4) 무료 Ops 범위 내에서 전체 흐름을 완성할 수 있기 때문이다.

> 가이드: "선정 이유"는 최소 2가지 이상, 가능하면 다른 도구 대비 비교 근거로 서술하면 평가에서 설득력이 높아집니다.

### 3. 워크플로우 흐름 설명

1. Trigger: Google Sheets – 새 리뷰 행 추가 감지
2. Router: 별점 3점 이하(부정) / 4점 이상(긍정) 분기
3. Action 1(부정 경로): Claude/ChatGPT 모듈 – 리뷰 3줄 요약 + 감정 키워드 추출
4. Action 2(부정 경로): Slack – 요약 내용과 함께 담당자 멘션 긴급 알림
5. Action(긍정 경로): Slack 아카이브 채널에 단순 기록
6. Error Handler: 시나리오 실행 실패 시 Gmail로 오류 알림 발송, Sheets 기록 실패 시 임시 백업 시트에 대체 저장

### 4. 구현 화면

> 가이드: 이 아래에 실제 Make 시나리오 캔버스 전체 화면 캡처를 삽입하세요. Router 조건식, AI 모듈 프롬프트 설정 화면, Slack 액션 설정 화면을 각각 캡처하면 좋습니다. API Key 등 민감정보는 마스킹(`***`) 처리하세요.

[스크린샷 삽입: Make 시나리오 전체 구성] [스크린샷 삽입: Router 조건 설정] [스크린샷 삽입: AI 요약 모듈 설정]

### 5. 실행 결과

> 가이드: 별점 2점 테스트 리뷰 1건, 별점 5점 테스트 리뷰 1건을 각각 입력해 두 분기 모두 최소 1회 실행된 로그를 캡처하세요. AI 요약 결과와 Slack 알림 메시지가 실제로 도착한 화면도 함께 넣으세요.

[스크린샷 삽입: 부정 리뷰 분기 실행 로그 + Slack 알림 도착 화면] [스크린샷 삽입: 긍정 리뷰 분기 실행 로그] [스크린샷 삽입: 의도적 오류 발생 시 실패 알림 이메일 도착 화면]

### 6. 보너스 반영 사항

AI 연동 Action(리뷰 요약·감정 키워드 추출)과 실패 알림·대체 저장 로직을 모두 하나의 Make 시나리오 안에 구현해 보너스 1, 2를 함께 충족했다.

---

> 문서 마지막 안내: 위 보고서에서 `>`로 시작하는 모든 줄(이 줄 포함)을 삭제하면 제출 가능한 최종 보고서만 남습니다.